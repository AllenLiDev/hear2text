import React from 'react';
import './Sidebar.css';

const Sidebar = (props) => {
    return (
        <nav className="sidebar">
            <div className="sidebar-stickey">
                side
            </div>
        </nav>
    )
}

export default Sidebar;